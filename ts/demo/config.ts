import { Cluster } from "../constants";

export const APP_CLUSTER = Cluster.Localhost;
export const NUM_BUYERS = 5;
export const ROUND_TIMEOUT_SECS = 60;
export const ENERGY_AMOUNT_DECIMALS = 1;

export const SERVER_PORT = 3100;
export const SERVER_URL = `http://127.0.0.1:${SERVER_PORT}`;
